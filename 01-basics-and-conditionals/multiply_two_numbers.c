//Multiply Two Numbers
#include<stdio.h>
int main() 
{
    int a, b, multiply;
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);
    multiply = a * b;
    printf("Multiplication = %d", multiply);
    return 0;
}